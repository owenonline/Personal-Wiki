# Copy this file to `.env` and fill in the values.
# `.env` is gitignored and must never be committed.

# Required only for the optional Claude integration test
# (tests/test_agent.py::test_run_live_against_real_api_*) and the manual
# smoke test in Plan 1 Task 10. The rest of the unit suite runs without it.
ANTHROPIC_API_KEY=
